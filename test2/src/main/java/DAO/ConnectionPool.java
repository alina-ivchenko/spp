package main.java.DAO;

import java.util.HashMap;

public class ConnectionPool {

    /*
    private ConnectionPool currPool = null;
    private HashMap<Boolean, SQLConnector> connectors;

    public ConnectionPool(int amountOfConnections) {
        int realAmountOfConnections = 1;
        if (amountOfConnections > realAmountOfConnections) realAmountOfConnections = amountOfConnections;

        if (currPool == null) {
            connectors = new HashMap<>(realAmountOfConnections);
        }
    }
    */
}
